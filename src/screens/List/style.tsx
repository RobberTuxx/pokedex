import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  title: {
    textAlign: 'center',
    color: 'white',
    fontSize: 22,
    fontWeight: '700',
    marginBottom: 10,
  },
});
